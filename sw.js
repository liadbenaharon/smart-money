const CACHE='smart-money-v17';
const APP_SHELL=['/smart-money/','/smart-money/index.html','/smart-money/style.css','/smart-money/app.js','/smart-money/recurring-fix.js','/smart-money/v030-features.js','/smart-money/manifest.json','/smart-money/version.json','/smart-money/CHANGELOG.md','/smart-money/icon-192-v2.png','/smart-money/icon-512-v2.png'];
self.addEventListener('install',event=>{event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll(APP_SHELL)));self.skipWaiting();});
self.addEventListener('activate',event=>{event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==CACHE).map(k=>caches.delete(k)))));self.clients.claim();});
self.addEventListener('fetch',event=>{if(event.request.method!=='GET')return;const url=new URL(event.request.url);if(url.origin!==self.location.origin)return;event.respondWith(fetch(event.request).then(response=>{if(response&&response.ok){const copy=response.clone();caches.open(CACHE).then(cache=>cache.put(event.request,copy));}return response;}).catch(async()=>{const cached=await caches.match(event.request);if(cached)return cached;if(event.request.mode==='navigate')return caches.match('/smart-money/index.html');return Response.error();}));});